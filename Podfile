# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ortiz-Koombea-fit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ortiz-Koombea-fit
	pod “Alamofire”
  pod 'ImageLoader'
  target 'ortiz-Koombea-fitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ortiz-Koombea-fitUITests' do
    # Pods for testing
  end

end
